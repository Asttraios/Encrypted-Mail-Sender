podzielic projekt na moduły
dodać opcje wysyłania do outlooka, wp, nie tylko gmail
dać szyfrowanie wiadomosci np. Playfaira
bedzie dzwiek you've got mail

def mail_choice(user_mail_choice):
    match user_mail_choice:
        case 1:
            print("gmail")
        case 2:
            print("outlook")
        case 3:
            print("wp")

pass_error = "Wrong password, try again!"
port = 465  # For SSL
sender_email = "jantestowypython@gmail.com"
receiver_email = "jantestowypython@gmail.com"


message = """\
Subject: Hi there

This message is sent from Python."""

password = input("Enter your passwod: ")
# Send email here

# Create a secure SSL context
context = ssl.create_default_context()

with smtplib.SMTP_SSL("smtp.gmail.com", port, context=context) as server:
    try:
        server.login("jantestowypython@gmail.com", password)
        # TODO: Send email here
        server.sendmail(sender_email, receiver_email, message)
    except: 
        print(pass_error)
    
   